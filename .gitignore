.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri build artifacts (multi-GB; never commit)
/src-tauri/target

# Built app binaries — distribute via GitHub Releases, not the repo
/dist/*.dmg

# Local, machine-specific Claude Code settings
.claude/settings.local.json

# Local git worktree checkouts
/.worktrees/
